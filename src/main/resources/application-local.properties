spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:voiceshield}
spring.datasource.username=${POSTGRES_USER:voiceshield}
spring.datasource.password=${POSTGRES_PASSWORD:voiceshield}
spring.datasource.driver-class-name=org.postgresql.Driver
